{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npm run prisma:generate && npm run build && cd ../admin-panel && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && sh startup.sh",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}