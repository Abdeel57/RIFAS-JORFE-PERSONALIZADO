{
  "name": "rifas-nao-monorepo",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install": "cd backend && npm install && cd ../admin-panel && npm install",
    "prisma:generate": "cd backend && npx prisma generate",
    "clean": "echo 'Cleaning...' && cd backend && rm -rf dist && cd ../admin-panel && rm -rf dist",
    "build": "cd backend && npx prisma generate && npm run build && cd ../admin-panel && npm run build",
    "start": "cd backend && sh startup.sh"
  }
}