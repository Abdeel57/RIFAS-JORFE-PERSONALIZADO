{
  "name": "rifas-nao-project",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npm run build && cd ../admin-panel && npm install && npm run build",
    "start": "cd backend && sh startup.sh"
  }
}